# Plan 02-02: Assessment Flow UI

```yaml
wave: 2
depends_on:
  - 02-01
files_modified:
  - app/(protected)/assessment/page.tsx
  - app/(protected)/assessment/layout.tsx
  - app/(protected)/assessment/[section]/page.tsx
  - app/(protected)/assessment/complete/page.tsx
  - components/assessment/progress-bar.tsx
  - components/assessment/question-card.tsx
  - components/assessment/likert-response.tsx
  - components/assessment/multiple-choice-response.tsx
  - components/assessment/citation-display.tsx
  - components/assessment/section-navigation.tsx
  - hooks/use-assessment.ts
  - actions/assessment.ts
requirements:
  - ASSESS-01
  - ASSESS-02
  - ASSESS-03
  - ASSESS-04
  - ASSESS-05
  - ASSESS-07
  - ASSESS-09
autonomous: true
estimated_duration: 45-55 minutes
```

## Objective

Create the assessment intake flow with progress tracking, question rendering, and auto-save functionality. Users complete a 45-60 minute assessment across four sections (Deen, Dunya, Aila, Nafs) with scholarly citations visible for each question.

## Tasks

### Task 1: Create Assessment Page Structure

<read_first>
- app/(protected)/dashboard/page.tsx (for layout pattern)
- .planning/phases/02-assessment-engine/02-CONTEXT.md (D-16, D-17)
</read_first>

<action>
Create the assessment page structure:

1. `app/(protected)/assessment/page.tsx`:
   - Assessment introduction/landing page
   - Show estimated time (45-60 minutes)
   - Explain the four sections
   - "Begin Assessment" button
   - Show "Resume Assessment" if in progress

2. `app/(protected)/assessment/layout.tsx`:
   - Shared layout for assessment pages
   - Progress bar at top
   - Section navigation indicators
   - Save indicator ("Progress saved")

3. `app/(protected)/assessment/[section]/page.tsx`:
   - Dynamic route for each section (deen, dunya, aila, nafs)
   - Renders questions for the current section
   - Handles navigation between sections

4. `app/(protected)/assessment/complete/page.tsx`:
   - Completion celebration page
   - Summary of sections completed
   - Link to results page
</action>

<acceptance_criteria>
- `app/(protected)/assessment/page.tsx` exists with introduction content
- `app/(protected)/assessment/layout.tsx` exists with progress UI
- `app/(protected)/assessment/[section]/page.tsx` exists with dynamic routing
- `app/(protected)/assessment/complete/page.tsx` exists with completion message
- All pages use protected route pattern from Phase 1
</acceptance_criteria>

---

### Task 2: Create Progress Bar Component

<read_first>
- components/ui/progress.tsx (if exists, or create)
- .planning/phases/02-assessment-engine/02-CONTEXT.md (D-16)
</read_first>

<action>
Create `components/assessment/progress-bar.tsx`:

1. Visual progress bar showing overall completion percentage
2. Section indicators below the bar:
   - Deen: ✓ (completed) | ● (in progress) | ○ (pending)
   - Dunya: same pattern
   - Aila: same pattern
   - Nafs: same pattern
3. Estimated time remaining based on questions answered
4. Animated transitions when progress updates

Props:
- `currentSection: string`
- `questionsAnswered: number`
- `totalQuestions: number`
- `sectionsCompleted: string[]`
</action>

<acceptance_criteria>
- `components/assessment/progress-bar.tsx` exists
- Component shows percentage complete
- Component shows section status indicators
- Component calculates estimated time remaining
- Component has smooth animations
</acceptance_criteria>

---

### Task 3: Create Question Card Component

<read_first>
- components/ui/card.tsx
- .planning/phases/02-assessment-engine/02-CONTEXT.md (D-04, D-05)
</read_first>

<action>
Create `components/assessment/question-card.tsx`:

1. Card container with question text prominently displayed
2. Citation display area (expandable)
3. Response area (delegates to Likert or Multiple Choice component)
4. Skip button with "I'd rather not answer" text
5. Navigation buttons (Previous, Next)
6. Question counter (e.g., "Question 15 of 40")

Props:
- `question: Question` (id, text, type, citation)
- `questionNumber: number`
- `totalInSection: number`
- `value: ResponseValue | null`
- `onChange: (value: ResponseValue) => void`
- `onSkip: () => void`
- `onNext: () => void`
- `onPrevious: () => void`
- `canGoBack: boolean`
</action>

<acceptance_criteria>
- `components/assessment/question-card.tsx` exists
- Component displays question text clearly
- Component includes skip functionality
- Component shows question counter
- Component handles navigation
</acceptance_criteria>

---

### Task 4: Create Response Input Components

<read_first>
- components/ui/radio-group.tsx (if exists, or create)
- components/ui/slider.tsx (if exists, or create)
- .planning/phases/02-assessment-engine/02-CONTEXT.md (D-04)
</read_first>

<action>
Create response input components:

1. `components/assessment/likert-response.tsx`:
   - 5-point Likert scale (1-5)
   - Labels: "Strongly Disagree" to "Strongly Agree"
   - Visual radio buttons or slider
   - Keyboard navigation support
   - Props: `value`, `onChange`, `disabled`

2. `components/assessment/multiple-choice-response.tsx`:
   - Radio group for single selection
   - Options from question.options
   - Props: `value`, `onChange`, `options`, `disabled`

3. Add shadcn/ui components if not present:
   - `npx shadcn@latest add radio-group slider -y`
</action>

<acceptance_criteria>
- `components/assessment/likert-response.tsx` exists
- Likert component shows 5-point scale with labels
- `components/assessment/multiple-choice-response.tsx` exists
- Multiple choice renders options dynamically
- Both components support keyboard navigation
</acceptance_criteria>

---

### Task 5: Create Citation Display Component

<read_first>
- .planning/phases/02-assessment-engine/02-CONTEXT.md (D-05, D-06)
</read_first>

<action>
Create `components/assessment/citation-display.tsx`:

1. Expandable/collapsible citation section below question
2. Shows citation type icon (📖 for Quran, 📚 for Hadith)
3. Displays:
   - Source (e.g., "Surah Al-Baqarah" or "Sahih Bukhari")
   - Reference (e.g., "2:177" or "Book 1, Hadith 1")
   - Arabic text (RTL support)
   - English translation
4. Scholar verified badge (optional, for future use)
5. "View source" link for verification (optional)

Props:
- `citation: Citation`
- `expanded?: boolean`
- `onToggle?: () => void`
</action>

<acceptance_criteria>
- `components/assessment/citation-display.tsx` exists
- Component shows citation source and reference
- Component displays Arabic text with RTL support
- Component displays English translation
- Component is expandable/collapsible
</acceptance_criteria>

---

### Task 6: Create Assessment Hook

<read_first>
- hooks/use-couple.ts (for pattern reference)
- lib/database/assessments.ts
- .planning/phases/02-assessment-engine/02-CONTEXT.md (D-02, D-17)
</read_first>

<action>
Create `hooks/use-assessment.ts`:

1. State management for assessment flow:
   - Current assessment data
   - Current section and question index
   - Responses (local state, synced to server)
   - Loading and error states

2. Functions:
   - `startAssessment()` - create new assessment
   - `resumeAssessment()` - load existing in-progress
   - `saveResponse(questionId, value)` - auto-save to server
   - `goToNextQuestion()` - navigate forward
   - `goToPreviousQuestion()` - navigate backward
   - `skipQuestion()` - mark as skipped
   - `completeSection()` - mark section complete
   - `completeAssessment()` - finalize all sections

3. Auto-save implementation:
   - Debounce saves (500ms delay)
   - Show "Saving..." indicator
   - Show "Saved" confirmation
</action>

<acceptance_criteria>
- `hooks/use-assessment.ts` exists
- Hook manages assessment state
- Hook provides navigation functions
- Hook implements auto-save with debounce
- Hook handles loading and error states
</acceptance_criteria>

---

### Task 7: Create Server Actions

<read_first>
- actions/auth.ts (for pattern reference)
- lib/database/assessments.ts
</read_first>

<action>
Create `actions/assessment.ts` with server actions:

1. `getAssessmentAction()` - fetch user's assessment status
2. `startAssessmentAction()` - create new assessment
3. `saveResponseAction(formData)` - save a response
4. `updateProgressAction(formData)` - update current section
5. `completeAssessmentAction(formData)` - finalize with scores

All actions should:
- Use server-side Supabase client
- Validate user authentication
- Return typed responses
- Handle errors gracefully
- Log to audit (where appropriate)
</action>

<acceptance_criteria>
- `actions/assessment.ts` exists
- File exports all required server actions
- All actions validate authentication
- All actions return typed responses
- Actions use revalidatePath where appropriate
</acceptance_criteria>

---

### Task 8: Update Dashboard with Assessment CTA

<read_first>
- app/(protected)/dashboard/page.tsx
</read_first>

<action>
Update the dashboard Assessment card:

1. Check if user has completed assessment
2. If not started: "Begin your assessment" button
3. If in progress: "Continue assessment" with progress indicator
4. If completed: "View your results" button
5. Remove "Coming in Phase 2" disabled state

Import and use the assessment status from the database.
</action>

<acceptance_criteria>
- Dashboard shows correct assessment state
- "Begin Assessment" button navigates to /assessment
- "Continue Assessment" shows progress percentage
- "View Results" navigates to results page
- No "Coming Soon" or disabled states
</acceptance_criteria>

---

## Verification Criteria

1. **Navigation Flow**: User can navigate through all sections
2. **Progress Tracking**: Progress bar accurately reflects completion
3. **Auto-Save**: Responses persist after page refresh
4. **Citations**: Every question shows scholarly citation
5. **Solo Mode**: Assessment works without partner

## Must Haves (Goal-Backward Verification)

- [ ] Assessment landing page with introduction
- [ ] Question flow with four sections
- [ ] Progress bar with section indicators
- [ ] Response inputs (Likert and multiple choice)
- [ ] Citation display for each question
- [ ] Auto-save functionality
- [ ] Dashboard integration

## Notes for Executor

- Questions will be seeded separately (admin task after schema creation)
- Use optimistic updates for better UX, fallback to server state
- Handle browser refresh gracefully (restore from saved state)
- Keyboard shortcuts: Arrow keys for Likert, Enter to continue
