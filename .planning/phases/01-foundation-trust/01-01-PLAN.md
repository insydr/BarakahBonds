# Plan 01-01: Project Setup & Supabase Configuration

```yaml
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tailwind.config.ts
  - tsconfig.json
  - .env.local.example
  - lib/supabase/client.ts
  - lib/supabase/server.ts
  - lib/supabase/types.ts
  - middleware.ts
  - supabase/migrations/001_initial_schema.sql
  - app/layout.tsx
  - app/globals.css
  - components/ui/*.tsx
requirements:
  - AUTH-01
  - AUTH-05
autonomous: true
estimated_duration: 35-45 minutes
```

## Objective

Initialize the Next.js 15 project with Supabase configuration, database schema with Row-Level Security, and core UI infrastructure. This establishes the technical foundation for all authentication and privacy features.

## Tasks

### Task 1: Initialize Next.js 15 Project

<read_first>
- .planning/research/STACK.md
- .planning/phases/01-foundation-trust/01-CONTEXT.md
</read_first>

<action>
Initialize a new Next.js 15 project with TypeScript, Tailwind CSS 4, and the App Router. Run: `npx create-next-app@latest barakah --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --turbopack`. Configure package.json with name "barakah" and description "Personal growth and reflection platform".
</action>

<acceptance_criteria>
- `package.json` contains `"name": "barakah"`
- `package.json` contains `"description": "Personal growth and reflection platform"`
- `next` dependency version starts with `"15"`
- `typescript` dependency version starts with `"5"`
- `tailwindcss` dependency version starts with `"4"`
- `app/layout.tsx` file exists
- `app/globals.css` file exists with Tailwind directives
</acceptance_criteria>

---

### Task 2: Install Core Dependencies

<read_first>
- package.json
- .planning/phases/01-foundation-trust/01-RESEARCH.md (Section 6.1)
</read_first>

<action>
Install Supabase client libraries, shadcn/ui prerequisites, and form handling packages. Run: `npm install @supabase/supabase-js @supabase/ssr react-hook-form @hookform/resolvers zod class-variance-authority clsx tailwind-merge date-fns`. Then run: `npm install -D @types/node`.
</action>

<acceptance_criteria>
- `package.json` dependencies include `"@supabase/supabase-js"`
- `package.json` dependencies include `"@supabase/ssr"`
- `package.json` dependencies include `"react-hook-form"`
- `package.json` dependencies include `"zod"`
- `package.json` dependencies include `"class-variance-authority"`
- `package.json` dependencies include `"clsx"`
- `package.json` dependencies include `"tailwind-merge"`
- `package.json` devDependencies include `"@types/node"`
</acceptance_criteria>

---

### Task 3: Initialize shadcn/ui

<read_first>
- package.json
- app/globals.css
- tsconfig.json
</read_first>

<action>
Initialize shadcn/ui with the neutral color scheme for discrete branding. Run: `npx shadcn@latest init -d`. Then add core components: `npx shadcn@latest add button input label card dialog -y`. Update `components.json` to use the default neutral theme.
</action>

<acceptance_criteria>
- `components.json` file exists
- `components/ui/button.tsx` file exists
- `components/ui/input.tsx` file exists
- `components/ui/label.tsx` file exists
- `components/ui/card.tsx` file exists
- `components/ui/dialog.tsx` file exists
- `lib/utils.ts` file exists with `cn` utility function
</acceptance_criteria>

---

### Task 4: Create Environment Configuration

<read_first>
- .planning/phases/01-foundation-trust/01-RESEARCH.md (Section 6.2)
</read_first>

<action>
Create `.env.local.example` file with required Supabase environment variable placeholders. Include: `NEXT_PUBLIC_SUPABASE_URL=`, `NEXT_PUBLIC_SUPABASE_ANON_KEY=`, `SUPABASE_SERVICE_ROLE_KEY=` (marked as server-only). Add `.env.local` to `.gitignore`.
</action>

<acceptance_criteria>
- `.env.local.example` file exists
- `.env.local.example` contains `NEXT_PUBLIC_SUPABASE_URL=`
- `.env.local.example` contains `NEXT_PUBLIC_SUPABASE_ANON_KEY=`
- `.env.local.example` contains `SUPABASE_SERVICE_ROLE_KEY=`
- `.gitignore` contains `.env.local`
</acceptance_criteria>

---

### Task 5: Create Supabase Client Utilities

<read_first>
- .planning/phases/01-foundation-trust/01-RESEARCH.md (Section 1.2)
</read_first>

<action>
Create `lib/supabase/client.ts` with the browser client pattern using `createBrowserClient` from `@supabase/ssr`. Create `lib/supabase/server.ts` with the server client pattern using `createServerClient` from `@supabase/ssr` and Next.js 15 `cookies()` API. Create `lib/supabase/types.ts` with Database type interface placeholder.
</action>

<acceptance_criteria>
- `lib/supabase/client.ts` file exists
- `lib/supabase/client.ts` exports `createClient` function
- `lib/supabase/client.ts` imports from `@supabase/ssr`
- `lib/supabase/server.ts` file exists
- `lib/supabase/server.ts` exports async `createClient` function
- `lib/supabase/server.ts` imports `cookies` from `next/headers`
- `lib/supabase/types.ts` file exists
- `lib/supabase/types.ts` exports `Database` type
</acceptance_criteria>

---

### Task 6: Create Database Migration with RLS Policies

<read_first>
- .planning/phases/01-foundation-trust/01-RESEARCH.md (Sections 2.1, 2.2, 2.3)
</read_first>

<action>
Create `supabase/migrations/001_initial_schema.sql` with: profiles table (id, display_name, avatar_url, timezone, privacy_settings, timestamps), couples table (id, partner_1_id, partner_2_id, invitation_code, invitation_expires_at, status, timestamps), audit_logs table (id, user_id, action, resource_type, resource_id, ip_address, user_agent, metadata, timestamps). Include all indexes, RLS enable statements, RLS policies for profiles, couples, and audit_logs, helper functions `get_partner_id()` and `has_active_couple()`, and the `handle_new_user()` trigger for auto-creating profiles.
</action>

<acceptance_criteria>
- `supabase/migrations/001_initial_schema.sql` file exists
- File contains `CREATE TABLE public.profiles`
- File contains `CREATE TABLE public.couples`
- File contains `CREATE TABLE public.audit_logs`
- File contains `ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY`
- File contains `ALTER TABLE public.couples ENABLE ROW LEVEL SECURITY`
- File contains `ALTER TABLE public.audit_logs ENABLE ROW LEVEL SECURITY`
- File contains `CREATE POLICY "Users can view own profile"`
- File contains `CREATE POLICY "Partners can view each other's profiles"`
- File contains `CREATE POLICY "Users can view own couple"`
- File contains `CREATE FUNCTION public.handle_new_user()`
- File contains `CREATE TRIGGER on_auth_user_created`
- File contains `CREATE FUNCTION get_partner_id()`
- File contains `CREATE FUNCTION has_active_couple()`
</acceptance_criteria>

---

### Task 7: Create Middleware for Auth State Management

<read_first>
- .planning/phases/01-foundation-trust/01-RESEARCH.md (Section 1.3)
</read_first>

<action>
Create `middleware.ts` at project root with Supabase SSR integration. Implement session refresh via `supabase.auth.getSession()`. Define protected paths array: `/dashboard`, `/settings`, `/assessment`, `/journal`. Redirect unauthenticated users from protected paths to `/auth/login` with redirect query param. Redirect authenticated users away from `/auth/login` and `/auth/register` to `/dashboard`. Use the matcher pattern that excludes static files.
</action>

<acceptance_criteria>
- `middleware.ts` file exists at project root
- File exports async `middleware` function
- File imports `createServerClient` from `@supabase/ssr`
- File imports `NextResponse` and `NextRequest` from `next/server`
- File contains `protectedPaths` array with `/dashboard`, `/settings`, `/assessment`, `/journal`
- File redirects to `/auth/login` for unauthenticated protected path access
- File contains `export const config` with matcher pattern
</acceptance_criteria>

---

### Task 8: Update Root Layout with Discrete Branding

<read_first>
- app/layout.tsx
- .planning/phases/01-foundation-trust/01-RESEARCH.md (Section 5.5)
</read_first>

<action>
Update `app/layout.tsx` with metadata for discrete branding. Set title default to "Barakah" and template to "%s | Barakah". Set description to "Personal growth and reflection platform". Configure theme-color for neutral appearance. Import globals.css. Ensure the layout supports both auth and protected route groups.
</action>

<acceptance_criteria>
- `app/layout.tsx` exports `metadata` object
- metadata contains `title.default: "Barakah"`
- metadata contains `title.template: "%s | Barakah"`
- metadata contains `description: "Personal growth and reflection platform"`
- File imports `./globals.css`
</acceptance_criteria>

---

## Verification Criteria

1. **Project Structure**: All files exist in correct locations
2. **Dependencies**: All required packages in package.json
3. **Supabase Client**: Both client and server utilities work
4. **Database Schema**: Migration file is syntactically correct SQL
5. **Middleware**: Properly handles session refresh and route protection
6. **Branding**: Discrete naming throughout configuration

## Must Haves (Goal-Backward Verification)

- [ ] Next.js 15 project initialized with TypeScript
- [ ] Supabase client utilities for SSR
- [ ] Database schema with RLS policies ready for deployment
- [ ] Middleware handles auth state and protected routes
- [ ] Discrete branding ("Barakah") in package.json and layout
- [ ] shadcn/ui components available for auth forms

## Notes for Executor

- This plan creates the foundation - no Supabase project creation required yet
- Migration file will be executed when Supabase project is created
- Middleware pattern is critical for session persistence (AUTH-02)
- All branding uses "Barakah" for discretion (AUTH-07)
